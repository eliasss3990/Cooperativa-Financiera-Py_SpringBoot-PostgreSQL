# Cooperativa-Financiera-Py_SpringBoot-PostgreSQL

## Comando para copiar el archivo de configuración de entorno:
```bash
cp .env.example .env
```

## Comando para levantar la bd:
```bash
docker-compose up -d
```